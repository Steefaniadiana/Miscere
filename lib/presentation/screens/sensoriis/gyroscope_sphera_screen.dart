import 'package:flutter/material.dart';

class GyroscopeSpheraScreen extends StatelessWidget {
  const GyroscopeSpheraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold( 
      appBar: AppBar( 
        title: const Text('Giroscopio chulisimo'),
      ),
      body: const Center(
        child: Text('Holii'),
      ),
    );
  }
}